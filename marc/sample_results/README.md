# Sample result JSONs

Four real result files (pulled from FarmShare) so you can see the exact
schema `aggregate.py` / `rliable_report.py` consume **without running
anything**: top-level keys + a nested `eval` block (eval_return,
loo_drop, role_similarity, behavioral_sep). One vanilla + one marc for
each of Overcooked and MPE.
