# envs / caches
envs/
__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/

# results stay on FarmShare, not in git
results/
logs/
*.npz
/tmp/

# Modal partial-pull leftovers from earlier OOM-debug attempts — junk.
# The canonical clean SMAX pull lives in smax_pull/ (see RESULTS.md).
modal_results/

# keep: results_gifs/ (curated checkpoint rollouts) — tracked on purpose
